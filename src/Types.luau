--!strict
-- Branch Types — regenerated by Branch Studio from your segments and data
-- template. Without the plugin these permissive defaults keep everything typed as any.

local Types = {}

-- BRANCH_AUTO_GENERATED_START
export type Segments = { [string]: any }
export type SegmentName = string
export type GetSegment = (self: any, name: string) -> any
export type DataPath = string | { string }
export type PlayerData = { [string]: any }
-- BRANCH_AUTO_GENERATED_END

return Types
